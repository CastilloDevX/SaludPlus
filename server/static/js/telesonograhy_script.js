const start_stream = document.querySelector("#start-stream")
const stop_stream = document.querySelector("#stop-stream")


start_stream.addEventListener("click", () => {
    /*
    fetch('/start-streaming')
    .then(response => {
        console.log("HERE!")
        if (response) {
            alert('La grabación ha comenzado', response);
        } else {
            alert('Error al iniciar la grabación');
        }
    })
    .catch(error => alert('Error al iniciar la grabación'));
    */
})